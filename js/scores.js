import{requireDb}from"./supabase.js";
export async function loadMyScores(userId){if(!userId)return[];const db=requireDb(),all=[];for(let from=0;;from+=1000){const{data,error}=await db.from("user_scores").select("id,chart,score,official_score,manual_history_score,version_score,current_clear_status,medal_code,rank_code,source,updated_at,songs!inner(id,master_key,genre,title,artist,banner_url,light_level,normal_level,hyper_level,ex_level,version_id,game_versions(name,sort_order))").eq("user_id",userId).or("score.gt.0,medal_code.neq.none").order("updated_at",{ascending:false}).range(from,from+999);if(error)throw error;all.push(...(data||[]));if(!data||data.length<1000)break;}return all.map(row=>({...row,...row.songs,song_id:row.songs.id,id:row.id,version_name:row.songs.game_versions?.name||"未設定",version_order:row.songs.game_versions?.sort_order??9999,level:row.songs[`${row.chart.toLowerCase()}_level`]}));}
export async function loadScoreCatalog(userId){const db=requireDb(),songs=[];for(let from=0;;from+=1000){const{data,error}=await db.from("songs").select("id,master_key,genre,title,artist,banner_url,light_level,normal_level,hyper_level,ex_level,version_id,game_versions(name,sort_order)").order("title").range(from,from+999);if(error)throw error;songs.push(...(data||[]));if(!data||data.length<1000)break;}const registered=userId?await loadMyScores(userId):[],scoreMap=new Map(registered.map(row=>[`${row.song_id}|${row.chart}`,row]));return songs.flatMap(song=>[["LIGHT","light_level"],["NORMAL","normal_level"],["HYPER","hyper_level"],["EX","ex_level"]].filter(([,key])=>Number(song[key])>0).map(([chart,key])=>{const saved=scoreMap.get(`${song.id}|${chart}`);return saved||{...song,id:`catalog:${song.id}:${chart}`,song_id:song.id,chart,level:song[key],version_name:song.game_versions?.name||"未設定",version_order:song.game_versions?.sort_order??9999,score:0,official_score:0,manual_history_score:0,version_score:0,current_clear_status:"failed",medal_code:"none",rank_code:"none",source:"catalog"};}));}
const FAILED_MEDALS=new Set(["h","i","j","l","m","n","failed_15_16","failed_12_14","failed_0_11"]);
const CURRENT_VERSION="pop'n music 29 High☆Cheers!!";
const truncate2=value=>Math.floor((Number(value)||0)*100+Number.EPSILON)/100;
export function songPopClass(row,score=row?.version_score,currentClearStatus=row?.current_clear_status){const n=Math.max(0,Math.min(100000,Number(score)||0));if(n<50000)return 0;const level=Number(row?.level)||0,status=String(currentClearStatus||"failed").toLowerCase(),bonus=status==="perfect"?24:status==="full_combo"?17.5:status==="clear"?5:0;return truncate2(2.1*level+.00094*(n-50000)+bonus+23);}
export function popClassSelection(rows){const mapped=rows.filter(row=>Number(row.version_score)>0).map(row=>({...row,pop_class:songPopClass(row)})),sort=(a,b)=>b.pop_class-a.pop_class||Number(b.version_score||0)-Number(a.version_score||0)||String(a.title||"").localeCompare(String(b.title||""),"ja"),current=mapped.filter(row=>row.version_name===CURRENT_VERSION).sort(sort).slice(0,20),other=mapped.filter(row=>row.version_name!==CURRENT_VERSION).sort(sort).slice(0,40),hundredths=[...current,...other].reduce((sum,row)=>sum+Math.round(row.pop_class*100),0),total=Math.floor(hundredths/60)/100;return{current,other,total};}
export function rankFromScore(score,medalCode="none"){const n=Math.max(0,Math.min(100000,Number(score)||0)),medal=String(medalCode||"none").toLowerCase(),cleared=medal!=="none"&&!FAILED_MEDALS.has(medal);let result;if(n>=99000)result="S+";else if(n>=98000)result="S";else if(n>=95000)result="AAA";else if(n>=93000)result="AA+";else if(n>=90000)result="AA";else if(n>=86000)result="A+";else if(n>=82000)result="A";else if(n>=77000)result="B+";else if(n>=72000)result="B";else if(n>=62000)result="C";else if(n>=50000)result="D";else result="E";return !cleared&&["S+","S","AAA","AA+","AA"].includes(result)?"A+":result;}
export async function saveScore(songId,chart,historyScore,versionScore,medalCode,currentClearStatus){const history=Math.max(0,Math.min(100000,Number(historyScore)||0)),version=Math.max(0,Math.min(100000,Number(versionScore)||0)),{error}=await requireDb().rpc("save_manual_score_v3",{p_song_id:songId,p_chart:chart,p_history_score:history,p_version_score:version,p_medal_code:medalCode,p_rank_code:rankFromScore(version,medalCode),p_current_clear_status:currentClearStatus});if(error)throw error;}
export async function syncScores(records,onProgress){const rows=records.filter(x=>Number(x.score)>0||Number(x.version_score)>0||String(x.medal_code||"none").toLowerCase()!=="none"||String(x.rank_code||"none").toLowerCase()!=="none").map(x=>({...x,score:Math.max(0,Number(x.score)||0),version_score:Math.max(0,Number(x.version_score)||0),current_clear_status:["failed","clear","full_combo","perfect"].includes(String(x.current_clear_status||"").toLowerCase())?String(x.current_clear_status).toLowerCase():"failed"}));if(!rows.length)return{saved:0,unmatched:0,failed:0};const db=requireDb(),BATCH=100,WORKERS=2,MAX_RETRIES=4,queue=[];for(let i=0;i<rows.length;i+=BATCH)queue.push(rows.slice(i,i+BATCH));let saved=0,unmatched=0,failed=0,done=0,retries=0,cursor=0;const wait=ms=>new Promise(resolve=>setTimeout(resolve,ms));const report=()=>onProgress?.({done,total:rows.length,saved,unmatched,failed,retries});async function send(batch,attempt=0){let data,error;try{({data,error}=await db.rpc("sync_my_scores",{p_records:batch}));}catch(e){error=e;}if(error){const message=String(error.message||error),temporary=/statement timeout|canceling statement due to statement timeout|fetch|network|failed to fetch|load failed|timeout|502|503|504|429|too many requests|connection|gateway/i.test(message);if(temporary&&attempt<MAX_RETRIES){retries++;await wait(700*Math.pow(2,attempt));return send(batch,attempt+1);}if(batch.length>1){retries++;const half=Math.ceil(batch.length/2);await send(batch.slice(0,half));await send(batch.slice(half));return;}failed++;done++;report();console.error("score sync record failed",batch[0],error);return;}const result=data?.[0]||{saved:0,unmatched:batch.length};saved+=Number(result.saved)||0;unmatched+=Number(result.unmatched)||0;done+=batch.length;report();}report();await Promise.all(Array.from({length:Math.min(WORKERS,queue.length)},async()=>{for(;;){const index=cursor++;if(index>=queue.length)return;await send(queue[index]);}}));return{saved,unmatched,failed};}
