import{requireDb}from"./supabase.js";

export async function loadMyScores(userId){if(!userId)return[];const client=requireDb();const{data,error}=await client.from("user_scores").select("id,score,medal_code,rank_code,updated_at,songs!inner(id,master_key,level,genre,title,artist,chart,banner_url)").eq("user_id",userId).order("updated_at",{ascending:false}).limit(5000);if(error)throw error;return(data||[]).map(row=>({...row,...row.songs}));}
export function rankFromScore(score){const value=Math.max(0,Math.min(100000,Number(score)||0));if(value>=99000)return"S+";if(value>=98000)return"S";if(value>=95000)return"AAA";if(value>=93000)return"AA+";if(value>=90000)return"AA";if(value>=86000)return"A+";if(value>=82000)return"A";if(value>=77000)return"B+";if(value>=72000)return"B";if(value>=62000)return"C";if(value>=50000)return"D";return"E";}
export async function saveScore(songId,score,medalCode){const client=requireDb(),value=Math.max(0,Math.min(100000,Number(score)||0));const{data:{user}}=await client.auth.getUser();if(!user)throw new Error("ログインしてください。");const{error}=await client.from("user_scores").upsert({user_id:user.id,song_id:songId,score:value,medal_code:medalCode||"none",rank_code:rankFromScore(value),source:"manual"},{onConflict:"user_id,song_id"});if(error)throw error;}
export async function syncScores(records){const client=requireDb(),normalized=records.map(row=>({...row,rank_code:rankFromScore(row.score)}));const{data,error}=await client.rpc("sync_my_scores",{p_records:normalized});if(error)throw error;return data?.[0]||{saved:0,unmatched:records.length};}
